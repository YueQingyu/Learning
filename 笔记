一、神经网络基础
计算机视觉：进行图像分类任务，将图片用三维数组表示用于计算机图像识别，但存在着图片中的事物的照射角度，形状改变，部分遮挡，背景混入等挑战。
得分函数：将输入数据（如图片）映射到各个类别得分的函数。得分越高，说明越可能是该类。
K近邻算法：通过计算待分类样本与训练集中所有样本的距离，选取最近的K个样本进行多数投票，得票最多的类别即为预测结果。但K近邻不适用于做图像分类。
损失函数：损失函数接收模型的得分和真实标签，输出一个数值表示模型的错误程度，训练的目标就是最小化这个数值。

向前传播：把输入数据从输入层开始，逐层计算加权和与激活函数，最终得到输出结果（得分或概率）的过程。
流程：输入x → 线性变换Wx → 得分scores → Softmax → 概率 → 交叉熵损失L
softmax（归一化指数函数）：将多个类别的原始得分转换成概率，让所有类别概率相加等于1。
反向传播：从输出端（损失）开始，沿着网络反向逐层计算梯度，告诉每个参数应该往哪个方向调整。

神经网络整体架构：神经网络就是一层层神经元通过全连接堆叠起来，中间用非线性函数“打断”，最终把原始输入一步步转换成想要的输出。


二、CNN卷积神经网络模型
1. 卷积层
作用：提取局部特征（边缘、纹理、形状）。
计算：卷积核在输入上滑动，每个位置做对应元素相乘再求和。
公式：
output_size = (input_size + 2×padding - kernel_size) // stride + 1
超参数：kernel_size（核大小）、stride（步长）、padding（填充）、out_channels（核个数）。
多通道：输入通道数 = 每个卷积核的通道数；输出通道数 = 卷积核的个数。
2. 池化层
作用：降低特征图尺寸，减少参数量，增强平移不变性。
计算：在滑动窗口内取最大值（最大池化）或平均值（平均池化）。
公式（与卷积相同）：
output_size = (input_size - pool_size) // stride + 1
特点：没有可学习的参数，只降低空间尺寸，不改变通道数。
3. 全连接层
作用：将提取的特征映射到样本标记空间（做分类或回归）。
公式：
output = input · W^T + b
其中 input 是展平后的特征向量，W 是权重矩阵，b 是偏置。
输入要求：必须是二维矩阵 [batch_size, feature_dim]，因此需要先用 flatten 将多维特征图压平。
4. 激活函数
作用：引入非线性，使神经网络可以拟合任意函数。
常用函数：
◦ Sigmoid(x) = 1 / (1 + e^{-x}) → 输出 (0,1)，用于二分类输出层。
◦ ReLU(x) = max(0, x) → 计算简单，缓解梯度消失。
◦ Tanh(x) = (e^x - e^{-x})/(e^x + e^{-x}) → 输出 (-1,1)，均值为0。
5. 损失函数
作用：衡量模型预测值与真实标签之间的差距，是优化的目标。
交叉熵损失（分类任务）：
Loss = -∑ y_true · log(y_pred)
其中 y_pred 是 softmax 后的概率分布。
特点：预测越准（概率接近1），损失越小 → 0；预测越错，损失越大 → ∞。
6. 优化器
作用：根据损失函数的梯度更新参数，使损失逐渐减小。
公式：
W_new = W_old - learning_rate × ∂Loss/∂W
b_new = b_old - learning_rate × ∂Loss/∂b
反向传播：从输出层向输入层逐层计算梯度（链式求导法则）。
超参数：learning_rate（学习率，控制更新步长）、momentum（动量，加速收敛）。
